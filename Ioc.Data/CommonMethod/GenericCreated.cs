using Ioc.Core;

namespace Ioc.Data.CommonMethod
{
    public static class GenericCreated<TEntity> where TEntity : PublicBaseEntity
    {
        /* public static TEntity CreatedBy(TEntity entity, User user)
         {
             entity.CreatedBy = user.use;
             return entity;
         }*/
    }
}
